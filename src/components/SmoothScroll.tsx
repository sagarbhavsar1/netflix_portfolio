// SmoothScroll is now handled via CSS in index.css (scroll-behavior: smooth).
// This component is kept as a no-op so index.tsx doesn't break.
const SmoothScroll = () => null;
export default SmoothScroll;
